PORT=4000
JWT_SECRET=change-this-to-a-long-random-string

# Free Postgres connection string (Neon: neon.tech, or Supabase). Required —
# without this, users/trips are lost every time the free host restarts.
# Example: postgresql://user:pass@ep-xxxx.us-east-2.aws.neon.tech/pusula?sslmode=require
DATABASE_URL=
